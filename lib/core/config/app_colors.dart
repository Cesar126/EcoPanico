import 'package:flutter/material.dart';

class AppColors {
  // Primary colors
  static const Color primary = Color(0xff1565c0); // Dark Blue
  static const Color secondary = Color(0xff546e7a); // Modern Grey
  
  // Alert & status colors
  static const Color alert = Color(0xffd32f2f); // Emergency Red
  static const Color warning = Color(0xfff57c00); // Amber/Orange for Suspicious
  static const Color success = Color(0xff2e7d32); // Success Green
  
  // Background & surface colors
  static const Color background = Color(0xffffffff);
  static const Color surface = Color(0xfff5f5f5);
  
  // Text colors
  static const Color textDark = Color(0xff1a1a1a);
  static const Color textLight = Color(0xff757575);
  static const Color textOnPrimary = Color(0xffffffff);
}
